import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/photobooth/', // Base path agar aset dapat dimuat dengan benar di GitHub Pages
})
